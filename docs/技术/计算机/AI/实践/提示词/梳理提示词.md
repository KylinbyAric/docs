
# 第一阶段 Research
## 需求概述
- 背景：现在对重构新老系统 diff 差异做归因分析并尝试给出修复方案，`spruce`重构前的模块、`onboard`是重构后的模块。当前链路 通过上游模块做流量copy分发到 `spruce`、`onboard`做分别处理。两个模块逻辑隔离，数据库表`t_driver_process_info`结构一致。
- 问题：通过 diff 2 个模块的数据库 `t_driver_process_info` 获取的字段 `ext_info.cards` 值有差异。
  请重点回答：
- 造成字段`ext_info.cards`差异的归因；
- 归因相关链路涉及的核心类、方法、配置项、topic、DTO、数据库/缓存/外部接口；
- 解决差异的方案有哪些，在`onboard`上的修改成本和方案。

## 规则
- 你先只做 Research，不要做任何代码改动，也不要输出新需求实现方案。你的目标是：完整、深入地读懂当前“编辑助理”相关链路，并产出文档
- 请深度阅读相关代码、配置、消息链路、任务类型、日志和上下游调用，不要停留在函数表面；只有真正讲清楚入口、任务流转、MQ topic、消费者/生产者、录音转写调用链、关键词判定逻辑、审核结果回写方式，才算完成。若 research 不足以支撑后续设计，请继续挖掘，不要提前结束。
- 如果存在不确定点，不要猜测，请明确标注“待确认”，并给出继续排查建议。
- 链路和逻辑要用日志做交叉确认，使用 Chrome MCP 搜索关键`线上日志`，搜索时必须勾选 `集群`；关键位置请贴出能证明链路的日志片段，并说明这些日志分别证明了什么。

## 输出
- 生成《Research.md》，格式 Markdown，内容至少包括：背景与目标、端到端链路、关键入口、MQ/任务流转、旧链路、关键词审核逻辑、核心代码位置、关键日志证据、风险点/歧义点/待确认问题；
- 使用 Excalidraw MCP 画出流程图或时序图，把 模块、MQ、关键词判定、结果回写串起来；
- 将图以 `.excalidraw` 文件导出；

## 引用
- 线上日志：https://monitor.xiaojukeji.com/#/dashboard/logRetrival/trace?date=2026-03-24&idc=cn&monitorEnv=CHN&traceId=ec5d691c69c1e1cd45a6aa9710470403

